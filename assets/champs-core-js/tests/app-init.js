import { initCore } from '/../src/init.js';
import Loader from '/../src/modules/Loader.js';

document.addEventListener('DOMContentLoaded', () => {
    initCore(document);

    // // padrão do projeto
    // Loader.useTemplate('cornetas'); // ou 'minimal', 'cornetas', 'org'...
});
